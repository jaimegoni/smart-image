
import "./SmartImageAnchor.css";

export const SmartImageAnchor = ({imageNote, containerId})=>{

    const smartImageContainer = document.getElementById(containerId);

    const xOffset = smartImageContainer.offsetLeft;
    const yOffset = smartImageContainer.offsetTop;

    const containerWidth = smartImageContainer.offsetWidth;
    const containerHeight = smartImageContainer.offsetHeight;

    



}